---
layout: default
title: 后端工程
nav_order: 4
has_children: true
permalink: /docs/backend/
---

# 后端工程

用户在电商应用中选好商品和收货地址，点击“提交订单”，很快看到订单编号。这个简单结果背后，后端已经接收 HTTP 请求、识别用户、读取商品价格、检查库存、写入订单，并把业务结果转换成响应。

随着交易量增加，问题会继续出现：两名用户同时购买最后一件商品，谁能成功？支付回调重复到达，会不会重复确认订单？发送通知需要十秒，下单请求是否一直等待？一台服务重启，已经接收的工作怎样继续？

后端工程要让请求在多用户、多服务和故障中仍然产生可信的业务结果。后端工程师长期承担四类责任：

| 责任 | 要回答的问题 | 订单中的表现 |
| --- | --- | --- |
| 接收请求 | 谁想做什么，怎样返回明确结果？ | 用户提交商品、地址和优惠券，接口返回订单号 |
| 管理状态 | 系统现在相信什么，并发修改时怎样保持正确？ | 成交金额、订单状态和可售库存 |
| 协调工作 | 工作怎样跨越请求、时间和服务继续？ | 订单过期、支付确认、库存释放和通知 |
| 保障运行 | 流量、故障和版本变化出现时怎样维持服务？ | 超时、重试、限流、灰度和恢复 |

这四类责任构成后端知识的纵向结构。HTTP、数据库、缓存和消息队列只是完成责任所用的技术，不能脱离业务问题判断它们是否需要。

## 工程师怎样判断一个设计

同一个方案还要从几个方向检查：

| 判断 | 在订单系统中要问什么 |
| --- | --- |
| 正确性 | 金额是否可信，库存会不会超卖，重复回调会不会重复生效？ |
| 可靠性 | 网络超时、实例重启和消息重复时，已经接收的交易能否确认与恢复？ |
| 性能与容量 | 用户要等多久，数据库和队列能承受多少请求，哪个资源会先耗尽？ |
| 安全与边界 | 调用者是谁，地址和优惠券属于谁，客户端提交的哪些数据不能相信？ |
| 可演进性 | 规则、接口和 Schema 改变时，新旧版本能否同时工作并逐步迁移？ |

这些判断不会分别形成五套系统。缓存提高读取性能时，仍要检查旧数据会不会破坏正确性；引入队列提高可靠性时，也要考虑重复消费、积压容量和问题能否观测。

**后端设计就是在业务约束下，同时守住结果正确、故障可恢复和系统可持续变化。**系统规模较小时，结构清楚的单体应用完全可以做到这些；只有容量、团队协作或独立发布形成真实约束时，才需要拆分服务。

## 先看一次下单请求

创建订单可以从一个 HTTP 请求开始。API 取得当前用户，校验商品和地址，业务层读取可信价格并检查库存，再通过数据库事务保存订单与明细。成功后返回订单 ID，失败则给出客户端能够处理的错误。

这一过程要区分几种责任：入口层处理 HTTP，认证组件确认调用者，业务层判断交易是否合法，数据访问层读写数据库。责任清楚后，接口协议、交易规则和存储实现才能分别变化。

## 数据库保存的是业务事实

订单创建成功以后，数据库中的订单、价格快照和库存变化成为权威状态。商品详情页缓存、日志和消息都可以复制这些事实，却不能各自决定最后一件库存属于谁。

当两个请求同时购买一件商品时，只在代码中先查询“库存大于零”再执行扣减并不安全。另一个请求可能在两步之间修改库存。数据库约束、事务和并发控制要把交易条件落实到真正写入的时刻。

## 长工作离开请求继续运行

发送短信、更新推荐数据和生成交易报表不适合长期占用下单请求。后端可以先创建任务或事件，再由 Worker 在后台执行。用户得到的是已经提交的订单，后续工作是否完成则通过任务状态、通知或重试机制继续追踪。

队列提高了工作继续执行的机会，也带来重复投递、乱序和恢复问题。业务动作需要幂等，任务进度需要持久化，失败还要区分重试、等待和终止。

## 多个服务会一起失败

真实交易可能经过订单、库存、支付和优惠服务。一次超时只表示调用方没有及时取得结果，无法单独证明对方没有执行。盲目重试可能重复创建支付，完全不重试又会放大短暂故障。

后端需要为调用设置超时，按错误类型决定重试，通过幂等键和状态查询处理结果未知，并用日志、指标和 Trace 还原请求经过的服务。发布时还要兼容新旧版本，避免一次变更同时影响所有交易。

## 用一笔订单学习这张地图

前面的责任框架帮助定位知识，一笔订单则解释技术为什么出现。本章按业务问题逐步展开：

| 订单遇到的问题 | 文章 | 所在责任 | 重点判断 |
| --- | --- | --- | --- |
| 用户提交商品、地址和优惠券 | [一个后端请求如何完成](request-lifecycle/) | 接收请求 | 业务事实何时真正成立，接口应返回什么 |
| 两人争抢最后一件库存 | [后端怎样保存业务状态](business-state/) | 管理状态 | 并发请求怎样守住约束、事务与权威状态 |
| 订单等待支付并在到期后取消 | [一个请求做不完时怎么办](async-tasks/) | 协调工作 | 任务怎样离开请求继续，并在恢复后重新判断 |
| 消息没有发出或被重复投递 | [消息为什么会丢失或重复](message-delivery/) | 协调工作 | 网络交付不完美时，业务结果怎样保持正确 |

后续内容会继续沿支付调用、商品缓存和生产事故，逐步进入“保障运行”。每读完一篇文章，都可以回到两组问题重新定位：系统正在承担哪类责任，这个设计在正确性、可靠性、性能、安全和可演进性上作出了什么取舍。
